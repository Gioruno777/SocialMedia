spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3307/social_media
    username: appuser
    password: apppassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: secret-key-secret-key-secret-key-secret-key
  expiration: 86400000
cors:
  FRONTEND_URL: "http://127.0.0.1:5173"
